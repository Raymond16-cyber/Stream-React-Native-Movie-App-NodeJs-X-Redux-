import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  pageHeaderText: {
    fontSize: 22,
    marginBottom: 1.5,
     textAlign: "center",
     color: "white",
     fontWeight: 700
  },
});
